import React from 'react';

import {
    Typography,
} from '@material-ui/core';

export default function Intro() {
    return (
        <React.Fragment>
            <Typography color="secondary">Intro</Typography>
        </React.Fragment>
    );
};